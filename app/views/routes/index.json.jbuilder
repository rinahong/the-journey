# json.array! @routes, partial: 'routes/route', as: :route

# json.array! @routes do |route|
#   json.extract! route, :id, :latitude, :longitude, :trip_id, :created_at, :updated_at
#   json.title route.address
#   json.start route.start_date
#   json.end route.end_date
#   # json.url route_url(route, format: :html)
# end
